<%= form_for(@partner_company) do |f| %>
  <% if @partner_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partner_company.errors.count, "error") %> prohibited this partner_company from being saved:</h2>

      <ul>
      <% @partner_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
